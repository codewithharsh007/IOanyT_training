# Feature 1: Todo List API with Categories

## Client Request
"We need a simple API where users can create todos, organize them into categories (work, personal, shopping), set due dates, and mark them complete. Nothing fancy — just a clean REST API."

## Technical Constraints
- Python (Flask or FastAPI)
- SQLite
- Must include error handling
- Must include at least basic input validation
